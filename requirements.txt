# Pinned to the versions verified in this environment for reproducible
# fine-tuning and serving. Update intentionally, then re-test.
# NOTE: torch is pinned without the local CUDA build tag (e.g. +cu128); install
# the matching CUDA/CPU wheel from https://pytorch.org for your platform.

# Core ML frameworks
torch==2.10.0
transformers==5.3.0
datasets==4.6.1

# Vietnamese NLP preprocessing
vncorenlp==1.0.3
pyvi==0.1.1

# Training & Tracking
accelerate==1.13.0
wandb==0.25.0

# Data handling
pandas==3.0.1
numpy==2.4.3
scikit-learn==1.8.0
rapidfuzz==3.14.5
openpyxl==3.1.5 # rater sheets CSV <-> Excel (src/qa/sheet_convert.py)

# Synthetic data generation on Colab also needs bitsandbytes (4-bit);
# notebooks/datagen_colab.ipynb installs and pins it there.

# API & Serving
fastapi==0.135.1
uvicorn[standard]==0.41.0
pydantic==2.12.5
python-multipart==0.0.22

# Utilities
pyyaml==6.0.3
python-dotenv==1.2.2
tqdm==4.67.3
matplotlib==3.10.8
seaborn==0.13.2

# Keyword extraction
yake==0.7.3

# Development (linting, testing)
ruff==0.15.15
pandas-stubs==3.0.5.260914 # type hints only, for Pylance/pyright
pytest==9.0.2
httpx==0.28.1
jupyter==1.1.1
